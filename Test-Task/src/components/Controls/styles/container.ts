import styled from "styled-components";

export const ControlsContainer = styled.div`
    display: flex;
    flex-wrap: nowrap;
    width: auto;
    position: absolute;
    left: 0;
    height: 100%;
    z-index: 1;
`;
